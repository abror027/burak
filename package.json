{
  "name": "burak",
  "version": "1.0.0",
  "description": "This is burak project backend",
  "main": "scr/server.ts",
  "scripts": {
    "start": "ts-node src/server.ts",
    "start:dev": "nodemon --exec ts-node src/server.ts",
    "build": "tsc",
    "train": "nodemon train.js",
    "own": "nodemon ownTrain.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "GaryMIT",
  "license": "ISC",
  "dependencies": {
    "moment": "^2.30.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.0",
    "ts-node": "^10.9.1",
    "tsconfig-paths": "^4.2.0",
    "typescript": "^5.2.2"
  }
}
